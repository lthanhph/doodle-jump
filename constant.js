const GAME = { 
    CONTEXT: null,
    WIDTH: 500,
    HEIGHT: 1000, 
    LOOP_NUMB: 16,
    SPEED: 0,
};

const PLATFORM = {
    WIDTH: 120,
    HEIGHT: 35,
}

// gravity interval time
const GRAVITY_TIME = 50;

// doodler max speed
const MAX_SPEED = 13;

// doodler turn speed
const TURN_SPEED = 5;

const FONT = 'Comic Sans MS';
const FONT_COLOR = '#b45f06';